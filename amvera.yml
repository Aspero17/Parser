meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: General.py
  command: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
